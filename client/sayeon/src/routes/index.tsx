import * as React from "react";
import { BrowserRouter, Routes, Navigate } from "react-router-dom";

const Root: React.FC = () => (
  <BrowserRouter>
    <Routes></Routes>
  </BrowserRouter>
);

export default Root;
